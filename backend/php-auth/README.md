# HORDE PHP Auth API

Лёгкий PHP backend для обычного хостинга с MySQL. Сайт остаётся на GitHub Pages, а кабинет может обращаться к этому API по HTTPS.

На сервере хостинга рядом с `index.php` должен лежать приватный `config.php` с реальными доступами. Его нельзя коммитить.
